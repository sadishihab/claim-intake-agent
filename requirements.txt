# Python 3.14. Install with:
#   python -m venv venv && ./venv/bin/pip install -r requirements.txt
# Audio also needs the PortAudio system library:
#   sudo apt install libportaudio2

# agent
websockets==17.1
sounddevice==0.5.6
numpy==2.5.2
python-dotenv==1.2.3

# review panel
fastapi==0.141.1
uvicorn==0.52.4

# tests
pytest==9.1.1
